/*
 * QUESTION:
 *
 * Given a doubly linked list of n nodes sorted by values, the task is to remove duplicate nodes present in the linked list.
 *
 * Example 1:
 * Input:
 * n = 6
 * 1<->1<->1<->2<->3<->4
 * Output:
 * 1<->2<->3<->4
 * Explanation:
 * Only the first occurrence of the node with value 1 is retained, and the rest of the nodes with value 1 are deleted.
 *
 * Example 2:
 * Input:
 * n = 7
 * 1<->2<->2<->3<->3<->4<->4
 * Output:
 * 1<->2<->3<->4
 * Explanation:
 * Only the first occurrence of nodes with values 2, 3, and 4 are retained, and the rest of the repeating nodes are deleted.
 *
 * Find it: https://www.google.com/search?q=Given%20a%20doubly%20linked%20list%20of%20n%20nodes%20sorted%20by%20values%2C%20the%20task%20is%20to%20remove%20duplicate%20nodes
 */

// ---- write your solution below ----


