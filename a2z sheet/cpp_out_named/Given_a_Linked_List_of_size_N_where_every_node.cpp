/*
 * QUESTION:
 *
 * Given a Linked List of size N, where every node represents a sub-linked-list and contains two pointers:
 * (i) a next pointer to the next node,
 * (ii) a bottom pointer to a linked list where this node is the head.
 * Each of the sub-linked lists is in sorted order.
 * Flatten the Link List such that all the nodes appear in a single level while maintaining the sorted order.
 *
 * Note: The flattened list will be printed using the bottom pointer instead of the next pointer.
 *
 * Find it: https://www.google.com/search?q=Given%20a%20Linked%20List%20of%20size%20N%2C%20where%20every%20node%20represents%20a%20sub-linked-list%20and%20contains%20two%20pointers%3A%20%28i%29
 */

// ---- write your solution below ----


